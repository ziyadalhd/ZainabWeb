export default function Loading() {
  return (
    <main className="page-shell section-space" aria-live="polite">
      <p className="text-lg font-bold text-[var(--brand-green)]">جارٍ تحميل الصفحة…</p>
    </main>
  );
}
