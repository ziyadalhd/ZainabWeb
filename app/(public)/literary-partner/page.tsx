import type { Metadata } from "next";
import { ContentPlaceholderPage } from "@/components/ui/ContentPlaceholderPage";

export const metadata: Metadata = { title: "الشريك الأدبي" };

export default function LiteraryPartnerPage() {
  return <ContentPlaceholderPage title="الشريك الأدبي" />;
}
